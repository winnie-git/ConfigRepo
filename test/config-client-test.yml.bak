server:
  port: 8007 #\u7AEF\u53E3\u53F7
spring:
  application:
    name: config-client #\u670D\u52A1\u540D\u79F0
cloud:
  config:
    label: master
      discovery:
        enabled: true
        service-id: config-server
      enabled: true
      fail-fast: true
      profile: dev
  consul:
      host: localhost
      port: 8500
      discovery:
          ip_address: localhost
          port: ${server.port}
          instancde-id: config-client-${server.port}
          service-name: config-client

